.next/
node_modules/
.env.local
*.pem

.vercel

# TypeScript incremental build cache
*.tsbuildinfo

# Local debugging / Excel-export harness
scripts/

# Local PDF render artifacts (screenshot tooling)
scripts/out/

# Local Claude Code state
.claude/

# Serwist build artifacts (the SW + injected precache manifest are generated
# from src/app/sw.ts on every build — never hand-edit or commit them).
/public/sw.js
/public/sw.js.map
/public/swe-worker-*.js
